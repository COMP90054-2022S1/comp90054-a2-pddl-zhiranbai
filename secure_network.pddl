(define (domain secure_network)
   (:requirements :strips :typing)
   (:types agent
           information
           key
	   )
   (:predicates
   )
  
   (:action share
   )
   
   (:action encrypt
   )

   (:action decrypt
   )
)
